# <Module name> Constitution

<!--
Module-level CLAUDE.md, loaded on demand when Claude reads files
in this subtree. Inherits everything from the root CLAUDE.md.

Rules:
- Keep this file ≤100 lines. If it grows, push content back to root
  or extract to a referenced file.
- Do NOT duplicate content from the root. If a rule already lives
  in root, leave it there.
- Do NOT contradict the root. If you find a conflict, fix the root
  (the cross-cutting rule wins).
- Only include sections where THIS module has content that genuinely
  differs from or extends root. Omit any section that would just
  duplicate root.
-->

## What this module is

<!--
1-2 sentences. Just enough that Claude knows the module's role
within the larger project.

Example:
The frontend Angular app for SGEN. Consumes APIs from sgen-batch-services
via the generated TypeScript client. Renders the operator console.
-->

[fill in]

## Non-negotiable invariants
<!-- OPTIONAL. Only invariants specific to THIS module. Omit if none.
     If you can't think of any, omit the section — don't pad. -->

- [fill in — module-specific only]

## Conventions worth following
<!-- OPTIONAL. Only conventions specific to THIS module. Omit if none. -->

- [fill in — module-specific only]

## How to verify work is done
<!-- OPTIONAL. Only if THIS module has its own verification command
     distinct from root. Common case: per-package test command. -->

- [fill in — e.g. "`npm test --workspace=frontend` exits 0"]

## Domain glossary
<!-- OPTIONAL. Only terms specific to THIS module. Omit if all
     domain terms are already in the root glossary. -->

- [term] — [definition]
